vending_dbt:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: vending_dbt_db
      schema: dev
      warehouse: vending_dbt_wh
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: vending_dbt_db
      schema: prod
      warehouse: vending_dbt_wh
